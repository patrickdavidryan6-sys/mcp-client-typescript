# Optional if you use `ant auth login` (preferred).
# The SDK also accepts ANTHROPIC_AUTH_TOKEN.
# A set ANTHROPIC_API_KEY overrides ant auth profiles — unset stale keys.
# ANTHROPIC_API_KEY=sk-ant-...
